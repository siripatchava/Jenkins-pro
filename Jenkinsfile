pipeline {
    agent any
    stages {
        stage('code_checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/siripatchava/Jenkins-pro.git'
            }
        }
    }
}